fastapi>=0.100.0,<1.0
uvicorn[standard]>=0.22.0
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.2.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
python-dotenv>=1.0.0
pydantic>=2.0.0
numpy>=1.24.0
torch>=2.0.0
transformers>=4.30.0
requests>=2.28.0
rich>=13.0.0
edge-tts>=6.1.0
google-api-python-client>=2.169.0
google-auth-oauthlib>=1.2.2
google-auth-httplib2>=0.2.0
dateparser>=1.2.0

# --- Agentic tool-calling system (added with agent rebuild) ---
groq>=0.9.0

# --- Gemini secondary provider (OpenAI-compatible endpoint) ---
langchain-openai>=0.2.0
openai>=1.30.0
pyautogui>=0.9.54
pygetwindow>=0.0.9
pycaw>=20230317
psutil>=5.9.0
screen-brightness-control>=0.23.0
pyperclip>=1.9.0
comtypes>=1.4.0
send2trash>=1.8.0
winsdk>=1.0.0; sys_platform == 'win32'

# --- UI automation (the ui_* tools) ---
# These were missing from this file even though the entire UI-control engine
# depends on them: a clean `pip install -r requirements.txt` produced a build
# where every on-screen click failed with "pywinauto isn't installed".
pywinauto>=0.6.9; sys_platform == 'win32'
pywin32>=306; sys_platform == 'win32'

# Screen capture. Without Pillow, pyscreeze cannot import, pyautogui.screenshot()
# raises, and every vision verification returns "no screenshot" -- which is
# exactly what happened in production.
Pillow>=10.0.0
